{
  <% if (projectProxyQuestion) { %>
  "proxy": "<%= projectProxy %>", <% } %>
  "projectType": "<%= projectUsage %>",
  "src": {
    "src": "src/",
    "assets": "",
    "bower": "bower_components/",
    "templates": "templates/",
    "css": "scss/",
    "fonts": "fonts/",
    "js": {
      "base": "js/",
      "mysource": "my-source/",
      "json": "json/"
    },
    "images": {
      "base": "images/",
      "svg": {
        "base": "svg/",
        "single": "single/",
        "sprite": "sprite/"
      },
      "bitmap": {
        "cssimages": "cssimages/",
        "htmlimages": "htmlimages/"
      }
    }
  },
  "dist": {
    <% if (projectUsage == 'HTML Protoypes') { %>
    "dist": "dist/",
    "templates": "dist/",<% } if (projectUsage == 'Wordpress') { %>
    "dist": "dist/wp-content/themes/<%= projectName %>/",
    "templates": "dist/wp-content/themes/<%= projectName %>/",<% } if (projectUsage == 'Craft') { %>
    "dist": "dist/public/",
    "templates": "dist/public/", <% } if (projectUsage == 'laravel') { %>
    "dist": "dist/public/",
    "templates": "dist/resources/views/", <% } %>
    "css": "css/",
    "fonts": "fonts/",
    "js": "js/",
    "images": {
      "base": "images/",
      "svg": {
        "base": "svg/",
        "single": "single/",
        "sprite": "sprite/"
      },
      "bitmap": {
        "cssimages": "cssimages/",
        "htmlimages": "htmlimages/"
      }
    }
  }
}
